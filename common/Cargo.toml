[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
chrono = { workspace = true, features = ["serde"] }
